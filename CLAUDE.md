# musings 작성 규칙

- 노트 본문은 `_musings/<yyyy-mm-dd>-<slug>.md` 에 둔다. 루트에 두지 않는다 — 공개 레포라 루트 파일 목록이 곧 일기 제목 목록이 된다.
- 사용자가 구어체로 이야기한 내용을 서술체/집필체로 정리해서 파일에 작성한다.
- Claude가 추측하거나 해석한 내용(예: "왜 그렇게 됐을까"에 대한 답)은 **대화로만 말하고, 파일에는 쓰지 않는다.** 파일에는 사용자가 실제로 한 말만 정리해서 담는다.
